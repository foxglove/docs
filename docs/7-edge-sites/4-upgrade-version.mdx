---
title: Upgrade version
---

Upgrade the version of your edge site.

Use Helm to perform in-place upgrades of the edge site software – these commands will use values configured during installation:

```shell
$ helm repo update
$ helm upgrade foxglove-edge-site foxglove/edge-site \
    --namespace foxglove
```
