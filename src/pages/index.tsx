import Layout from "@theme/Layout";
import React, { ReactElement } from "react";

export default function index(): ReactElement {
  return (
    <Layout>
      <div style={{ padding: "5em", textAlign: "center" }}>Docs home</div>
    </Layout>
  );
}
